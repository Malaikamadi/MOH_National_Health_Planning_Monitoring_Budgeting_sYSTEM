/**
 * Auto-generated TypeScript types from the NHPMBR OpenAPI spec.
 *
 * Run `pnpm --filter @nhpmbr/shared-types generate` after the API is up
 * to refresh `./generated/api.ts`. The web and mobile apps will consume
 * those types instead of the hand-written shapes currently in
 * `apps/web/src/lib/api-client.ts`.
 */

export {};
